# Smart Bookmark manager

It solved the existing bookmark manager issue like not efficient sorting, searching on just one click instead of popping into new page. Also supports to add the URLs to the firebase DB.


### Setup
- After cloning, build this and upload the dist folder to the extension page 
- Add firebase keys for the DB persistence options, optional .